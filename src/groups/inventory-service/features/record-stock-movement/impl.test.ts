/**
 * [角色] 功能单元：record-stock-movement —— 验收测试（定稿区）
 * AI 的"完成标准"：AI 不得修改本文件（改了验收测试 = 作弊）。
 * TODO(人/功能规格评审)：功能规格定稿后，把不变量逐条翻译成测试。
 * 全部使用内存适配器（src/groups/<组>/adapters/memory/**），不依赖基础设施。
 */

import { describe, expect, it } from "vitest";
import { recordStockMovement } from "./impl";

describe("record-stock-movement 单元验收测试", () => {
  it("TODO: 不变量 1", async () => {
    // TODO: 组装内存适配器 → 调用 recordStockMovement → 断言结果/错误码
    expect(true).toBe(true);
  });
});
