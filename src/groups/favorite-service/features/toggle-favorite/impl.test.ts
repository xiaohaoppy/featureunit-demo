/**
 * [角色] 功能单元：toggle-favorite —— 判据（冻结区）
 * AI 的"完成标准"：AI 不得修改本文件（改了判据 = 作弊）。
 * TODO(人/契约评审)：契约冻结后，把不变量逐条翻译成测试。
 * 全部使用内存适配器（src/groups/<组>/adapters/memory/**），不依赖基础设施。
 */

import { describe, expect, it } from "vitest";
import { toggleFavorite } from "./impl";

describe("toggle-favorite 单元判据", () => {
  it("TODO: 不变量 1", async () => {
    // TODO: 组装内存适配器 → 调用 toggleFavorite → 断言结果/错误码
    expect(true).toBe(true);
  });
});
